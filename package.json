{
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "prepare": "pnpm run build"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "esbuild": "^0.25.0",
    "tsup": "^8.3.6",
    "typescript": "^5.7.3"
  }
}
